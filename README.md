# FLask and C++ web Servers | C++ Controller code runs Arduino for physcial functions
Flask app to allow users to select a color and send that hex code to C++ web server over the LAN via http request.
